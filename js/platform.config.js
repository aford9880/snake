/*
 * Конфигурация платформы для текущей сборки.
 *
 * Единственное место, где выбирается площадка. Файл перезаписывается
 * скриптом tools/build.mjs при сборке под конкретную платформу; в репозитории
 * лежит вариант для локальной разработки.
 *
 *   'auto'   — определить автоматически (есть SDK Яндекса → yandex, иначе mock)
 *   'mock'   — всегда локальная заглушка, без SDK
 *   'yandex' — Яндекс Игры
 */
export const platformConfig = {
  platform: 'auto',
  saveKey: 'snakeSave',
};
